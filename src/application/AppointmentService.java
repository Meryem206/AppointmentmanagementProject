package application;

import javafx.collections.FXCollections;
import javafx.collections.ObservableList;

import java.time.LocalDate;
import java.time.LocalTime;
/*
public class AppointmentService {

    // Observable list to hold appointments
    private ObservableList<Rendezvous> rendezvousList = FXCollections.observableArrayList();

    // Method to save the appointment (using Rendezvous class)
    public void saveAppointment(String patientName, String prenom, String adresse, String email, String sexe,
                                LocalDate dateNaissance, LocalDate dateRendezVous, LocalTime startTime,LocalTime endtime) {
       
		// Create a new Rendezvous object
        Rendezvous newRdv = new Rendezvous(patientName, prenom, adresse, email, sexe, dateNaissance, dateRendezVous, startTime,endtime);

        // Add the new appointment to the list (can be replaced with DB logic)
        rendezvousList.add(newRdv);

        System.out.println("Appointment saved:");
        System.out.println("Patient: " + patientName);
        System.out.println("Start Time: " + startTime);
        System.out.println("Appointment Date: " + dateRendezVous);
    }

    // Method to get all appointments
    public ObservableList<Rendezvous> getAllAppointments() {
        return rendezvousList;
    }
}*/
